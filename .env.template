# Network Share Credentials
# The SMB/CIFS server path (e.g., //192.168.1.100/Public or //nas.local/dronevideos)
SMB_SERVER=//192.168.1.100/Public

# SMB username for authentication
SMB_USER=your_username

# SMB password for authentication
SMB_PASS=your_password

# Optional: Override the mount point for USB drives (default: /mnt/usb_incoming)
# MOUNT_POINT=/mnt/usb_incoming

# Optional: Override the destination root path (default: /mnt/network_share)
# DEST_ROOT=/mnt/network_share

# Optional: Override the server port (default: 3000)
# PORT=3000

# Optional: Set NODE_ENV (set in docker-compose.yml as production)
# NODE_ENV=production
